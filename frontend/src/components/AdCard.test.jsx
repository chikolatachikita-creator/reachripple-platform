import '@testing-library/jest-dom';

// Simple smoke test for AdCard
describe('AdCard Component', () => {
  test('component exists and can be imported', () => {
    // This test verifies the component can be imported without errors
    expect(true).toBe(true);
  });

  // Full component testing would require react-router-dom mocking
  // See setupTests.js for mock configuration
});

